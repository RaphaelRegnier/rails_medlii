<%= form_tag users_path([:instrument_id]), method: :get do %>
  <%= select_tag "instrument_id", options_from_collection_for_select(Instrument.all,"id", "name")%>
  <%= submit_tag "go", name: nil, class: 'btn' %>
<% end %>