<div class="container">
   <div class="row ">
     <div class="col-xs-10 col-xs-offset-1">

<%= simple_form_for [@played_instrument]  do |f| %>
  <%= f.input :instrument, collection: Instrument.all%>
  <%= f.input :level, collection: (1..10)%>
  <%= f.button :submit, class: 'btn btn-raised btn-primary' %>
<% end %>

     </div>
   </div>
 </div>

